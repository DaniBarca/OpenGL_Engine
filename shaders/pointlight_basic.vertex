#version 330 core

layout(location = 0) in vec3 vertex_modelspace;
layout(location = 1) in vec3 normal_modelspace;

uniform mat4 PVM;

out vec3 fragmentColor;

void main(){

    gl_Position = PVM * vec4(vertex_modelspace,1);

	fragmentColor = normal_modelspace;

}